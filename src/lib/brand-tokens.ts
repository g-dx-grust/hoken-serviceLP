/**
 * Brand tokens for non-DOM renderers such as `next/og`.
 * These mirror the CSS custom properties defined in globals.css.
 */
export const BRAND_COLORS = {
  bg: "#0b2545",
  fg: "#ffffff",
  mutedOnBg: "rgba(255,255,255,0.76)",
  subtleOnBg: "rgba(255,255,255,0.14)",
  accent: "#8a6f3a",
} as const;
